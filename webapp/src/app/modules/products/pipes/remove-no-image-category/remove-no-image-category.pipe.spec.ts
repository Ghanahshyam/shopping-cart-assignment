import { RemoveNoImageCategoryPipe } from './remove-no-image-category.pipe';

describe('RemoveNoImageCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveNoImageCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
